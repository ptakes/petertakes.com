{
  "private": true,
  "name": "petertakes.com",
  "version": "0.1.0",
  "author": "Peter Takes",
  "scripts": {
    "build": "cross-env NODE_ENV=development npm run gulp build",
    "deploy": "cross-env NODE_ENV=production npm run gulp deploy",
    "clean": "npm run gulp clean",
    "e2e": "cross-env NODE_ENV=test npm run gulp e2e",
    "gulp": "cross-env TS_NODE_PROJECT=tsconfig.tsnode.json TS_NODE_FAST=true gulp",
    "preinstall": "dotnet restore",
    "postinstall": "typings install",
    "publish": "cross-env NODE_ENV=production npm run gulp publish",
    "serve": "cross-env NODE_ENV=development npm run gulp serve",
    "serve:dev": "cross-env NODE_ENV=development npm run gulp serve",
    "serve:prod": "cross-env NODE_ENV=production npm run gulp serve",
    "start": "npm run serve:dev",
    "test": "cross-env NODE_ENV=test npm run gulp test",
    "tsc": "tsc",
    "tslint": "tslint",
    "typings": "typings",
    "watch": "npm run serve:dev -- --watch",
    "watch:dev": "npm run serve:dev -- --watch",
    "watch:test": "npm run test -- --watch",
    "watch:prod": "npm run serve:prod -- --watch"
  },
  "dependencies": {
    "bluebird": "^3.4.6"
  },
  "devDependencies": {
    "cross-env": "^3.1.3",
    "del": "^2.2.2",
    "gulp": "github:gulpjs/gulp#4.0",
    "gulp-help-four": "^0.2.3",
    "require-dir": "^0.3.1",
    "ts-node": "^1.7.0",
    "tslint": "^3.15.1",
    "typescript": "^2.0.10",
    "typings": "^2.0.0",
    "yargs": "^6.4.0"
  }
}
